import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  navBarContainer: {
    position: "absolute",
    top: 0,
    left: 0,
    height: 50,
    width: "100%",
    alignItems: "center",
    zIndex: 9,
    flexDirection: 'row',
  },
});

export default styles;
